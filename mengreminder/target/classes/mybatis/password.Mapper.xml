<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ying.service.mybatis.PasswordMngservice" >
    <insert id="addItem" >
        INSERT into `password` (openId, appName, userName, `password`) VALUES (#{openId},#{appName},#{userName},#{password})
    </insert>
    <select id="queryAppName" >
    </select>
    <select id="queryUserName">
    </select>
    <select id="queryPassword">
    </select>
</mapper>